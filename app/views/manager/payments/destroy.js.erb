<% @payments = @project.payments.order(:created_at).page(params[:page]) %>;
$('#payment-list').empty();
$('#payment-list').append("<%= j render 'payments/paymentlist', url_type: 'manager', payments: @payments %>");
