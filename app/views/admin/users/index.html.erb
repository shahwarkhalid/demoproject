<div class="container">
  <div class="container">
    <%= render 'search', local: @roles %>
  </div>
  <div class="container" id="userlist">
    <%= render 'userlist', local: @users %>
    <%= paginate @users %>
  </div>
</div>
