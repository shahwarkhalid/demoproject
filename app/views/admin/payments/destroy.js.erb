<% @payments = @project.payments.order(:created_at).page(params[:page]) %>;
$('#payment-list').empty();
$('#payment-list').append("<%= j render 'payments/payment_list', url_type: 'admin', payments: @payments %>");
