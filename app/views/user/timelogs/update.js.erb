$('#my-modal').modal('hide');
alert('Timelog Updated Successfully');
<% @timelogs = @timelog.project.timelogs.order(:created_at).page(params[:page]) %>
$('#timelog-list').empty();
$('#timelog-list').append("<%= j render 'admin/timelogs/timelogslist', timelogs: @timelogs %>");
